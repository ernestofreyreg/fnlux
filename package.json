{
  "name": "fnux",
  "description": "Bare bones flux library for reusable complex components",
  "version": "0.0.1",
  "author": "Ernesto Freyre",
  "keywords": [
    "flux",
    "javascript",
    "react"
  ],
  "main": "src/index.js",
  "license": "BSD",
  "devDependencies": {
    "babel-core": "^6.8.0",
    "babel-preset-es2015": "^6.6.0",
    "babel-runtime": "^6.6.1",
    "expect.js": "^0.3.1",
    "mocha": "^2.4.5"
  },
  "scripts": {
    "lint": "./node_modules/eslint/bin/eslint.js ./dev/**/*.js ./src/**/*.js",
    "test:unit": "NODE_ENV=test mocha --timeout 8000 --compilers js:babel-register -b test/unit",
    "test": "npm run test:unit",
    "validate": "npm ls",
    "build-deploy": "NODE_ENV=production webpack --config dev/webpack.live.config.js"
  }
}
